export enum ETextFieldSize {
  small = 'small',
  large = 'large',
  medium = 'medium'
}

export enum ETextFieldType {
  text = 'text',
  email = 'email',
  password = 'password'
}

export enum EInputType {
  input = 'input',
  textArea = 'textArea'
}